import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2.57.4";
const allowed=new Set(["https://fit.fssolucoes.tech",...String(Deno.env.get("FSFIT_ALLOWED_ORIGINS")||"").split(",").map(v=>v.trim()).filter(Boolean)]);
const cors=(r:Request)=>{const o=r.headers.get("origin")||"";return{"Access-Control-Allow-Origin":allowed.has(o)?o:"https://fit.fssolucoes.tech","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"POST, OPTIONS","Vary":"Origin"}};
const originOk=(r:Request)=>!r.headers.get("origin")||allowed.has(r.headers.get("origin")||"");
const json=(r:Request,d:unknown,s=200)=>new Response(JSON.stringify(d),{status:s,headers:{...cors(r),"Content-Type":"application/json","Cache-Control":"no-store"}});
const env=(n:string)=>{const v=Deno.env.get(n);if(!v)throw new Error(`Secret ausente: ${n}`);return v};
const base=()=>String(Deno.env.get("EFI_ENV")||"production").toLowerCase().startsWith("prod")?"https://cobrancas.api.efipay.com.br":"https://cobrancas-h.api.efipay.com.br";
async function token(){const a=btoa(`${env("EFI_CLIENT_ID")}:${env("EFI_CLIENT_SECRET")}`);const r=await fetch(`${base()}/v1/authorize`,{method:"POST",headers:{Authorization:`Basic ${a}`,"Content-Type":"application/json"},body:JSON.stringify({grant_type:"client_credentials"})});const p=await r.json().catch(()=>({}));const t=p?.access_token||p?.data?.access_token;if(!r.ok||!t)throw new Error(p?.error_description||p?.message||"Falha na autorização Efí");return String(t)}
Deno.serve(async(req:Request)=>{if(req.method==="OPTIONS")return originOk(req)?new Response("ok",{headers:cors(req)}):json(req,{erro:"Origem não autorizada"},403);if(req.method!=="POST")return json(req,{erro:"Método não permitido"},405);if(!originOk(req))return json(req,{erro:"Origem não autorizada"},403);let admin:any=null,userId:string|null=null,a:any=null;try{const auth=req.headers.get("Authorization")||"";const uc=createClient(env("SUPABASE_URL"),env("SUPABASE_ANON_KEY"),{global:{headers:{Authorization:auth}}});const {data:u,error:ue}=await uc.auth.getUser();if(ue||!u.user)return json(req,{erro:"Não autenticado"},401);userId=u.user.id;admin=createClient(env("SUPABASE_URL"),env("SUPABASE_SERVICE_ROLE_KEY"));const {data:rate,error:re}=await admin.rpc("fsfit_consumir_rate_limit_assinatura",{p_escopo:"cancelar_cartao",p_sujeito:userId,p_limite:3,p_janela_segundos:120});if(re)throw re;if(!rate)return json(req,{erro:"Aguarde antes de tentar novamente."},429);const b=await req.json().catch(()=>({}));const id=String(b?.assinatura_id||"").trim(),remover=Boolean(b?.remover_cartao);if(!id)return json(req,{erro:"assinatura_id é obrigatório"},400);const {data:ass,error:ae}=await admin.from("assinaturas").select("id,personal_id,status,efi_subscription_id,renovacao_automatica,acesso_valido_ate,meio_pagamento,cartao_mascara").eq("id",id).eq("personal_id",userId).eq("meio_pagamento","cartao").single();if(ae||!ass)return json(req,{erro:"Assinatura não encontrada."},404);a=ass;const now=new Date().toISOString();if(!a.renovacao_automatica||a.status==="cancelada"){if(remover&&a.cartao_mascara){const {error}=await admin.from("assinaturas").update({cartao_mascara:null,updated_at:now}).eq("id",a.id);if(error)throw error}return json(req,{sucesso:true,ja_cancelada:true,cartao_removido:remover,acesso_valido_ate:a.acesso_valido_ate})}if(!a.efi_subscription_id)return json(req,{erro:"Assinatura sem vínculo com a Efí."},409);const t=await token();const r=await fetch(`${base()}/v1/subscription/${a.efi_subscription_id}/cancel`,{method:"PUT",headers:{Authorization:`Bearer ${t}`,"Content-Type":"application/json"}});const p=await r.json().catch(()=>({}));if(!r.ok&&!["canceled","cancelled"].includes(String(p?.data?.status||p?.status||"").toLowerCase()))throw new Error(p?.error_description||p?.message||p?.data?.message||"A Efí não confirmou o cancelamento.");const {error:up}=await admin.from("assinaturas").update({status:"cancelada",renovacao_automatica:false,cancelada_em:now,cancelamento_solicitado_em:now,proxima_cobranca_em:null,cartao_mascara:remover?null:a.cartao_mascara,updated_at:now}).eq("id",a.id).eq("personal_id",userId);if(up){await admin.rpc("fsfit_registrar_incidente_financeiro",{p_personal_id:userId,p_origem:"cancelar-assinatura-cartao-fsfit",p_tipo:"cartao_divergente",p_referencia_externa:String(a.efi_subscription_id),p_codigo_erro:"CANCELAMENTO_LOCAL_FALHOU",p_mensagem:up.message,p_contexto:{assinatura_id:a.id,remover_cartao:remover}});throw new Error("Cancelamento confirmado pela Efí e enviado para reconciliação no FS Fit.")}await admin.rpc("fsfit_registrar_evento_financeiro",{p_personal_id:userId,p_origem:"cancelar-assinatura-cartao-fsfit",p_tipo_evento:"assinatura_cartao_cancelada",p_referencia_externa:String(a.efi_subscription_id),p_assinatura_id:a.id,p_cobranca_id:null,p_status_anterior:a.status,p_status_novo:"cancelada",p_sucesso:true,p_codigo_erro:null,p_mensagem_resumida:null});return json(req,{sucesso:true,cartao_removido:remover,acesso_valido_ate:a.acesso_valido_ate,mensagem:"Renovação automática cancelada. O acesso permanece até o fim do período pago."})}catch(e){console.error("cancelar-assinatura-cartao-fsfit",e);return json(req,{erro:e instanceof Error?e.message:"Erro interno"},500)}});